{
  "name": "asteroid-avoider",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"bgBlue.bold.white,bgGreen.bold.black\" \"pnpm --filter server run dev\" \"pnpm --filter client run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
